class Birb {
  String favouriteGreeting = 'Henlo';
  String name = 'Birb';
  int age = 10;
  List favouriteFood = ['Seeb', 'Seeb', 'Seeb'];
  Map goodBadMap = {
        'good': ['Seeb'],
        'bad': 'rest',
        'goodCount': 1,
        'badCount': 'ERROR'
  };
}


List favouriteFood = Birb.favouriteFood;
Map goodBadMap = Birb.goodBadMap;

   class foo = Birb;
  void printDetails() {
    scremLn(foo.favouriteGreeting + " am " + foo.name);
    scremLn("I am " + foo.age + " years old");
    scremLn("I like " + favouriteFood[0]);

    // IDK, first thing that came to mind
    scremLn('There is only ' + goodBadMap['goodCount'] + " item in my good list");
  }

printDetails();
