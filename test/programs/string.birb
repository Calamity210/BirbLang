String henlo = "
Henlo Birb
";

henlo.trim(); // "Henlo Birb"

henlo.toUpperCase(); // "HENLO BIRB"

// "HENLO"
scremLn(henlo.substring(0, 5)); // output HENLO

List colors = ['c', 'o', 'l', 'o', 'r', 's'];

String colorStr = '';

for (int i = 0; i < colors.length; i++) {
  colorStr += '$' + (90 + i) + 'm' + colors[i];
}

scremLn(colorStr); // output [90mc[91mo[92ml[93mo[94mr[95ms

String birb = ' birbo\n';
scremF('henlo{0}', birb); // output henlo birbo

scremLn('\u221E;'); // output ∞